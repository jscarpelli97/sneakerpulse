# Free plan (€0 / 1,000 req/mo): https://kicks.dev/register — key must show as Active
KICKSDB_API_KEY=

# Optional: when set, GET /api/status with header x-status-token returns cache/fetch details
STATUS_TOKEN=
