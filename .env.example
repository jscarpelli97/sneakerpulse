# Free key from https://kicks.dev/register — used to load live StockX market data
KICKSDB_API_KEY=

# Optional: when set, GET /api/status with header x-status-token returns cache/fetch details
STATUS_TOKEN=
